{
  "name": "warp-claude-proxy",
  "version": "0.1.0",
  "description": "Local Anthropic-compatible API proxy that delegates to the Claude CLI — use your Claude Max subscription in Warp and other AI-aware IDEs",
  "type": "module",
  "bin": {
    "warp-claude-proxy": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
